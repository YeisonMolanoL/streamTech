import { YesNoPipelinePipe } from './yes-no-pipeline.pipe';

describe('YesNoPipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new YesNoPipelinePipe();
    expect(pipe).toBeTruthy();
  });
});
